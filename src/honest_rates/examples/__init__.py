"""Worked examples that use only the public `honest_rates` API — no `nearmiss`
import anywhere in this subpackage. They exist to prove the "usable on any
point-event dataset" claim, not just assert it in a docstring."""
