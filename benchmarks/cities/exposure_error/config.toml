# Generated by benchmarks/generator.py, regime "exposure_error". Do not hand-edit;
# re-run the generator instead. Lets any tool run nearmiss itself against this
# city via:
#   nearmiss analyze --config benchmarks/cities/exposure_error/config.toml
city = "benchmark-exposure_error"
streets = "streets.geojson"
reports = "reports.json"
exposure = "exposure.json"
raw_dir = "/tmp/nm-bench-exposure_error-raw"
out_dir = "/tmp/nm-bench-exposure_error-pub"
exposure_unit = "synthetic exposure units"
dataset_note = "SYNTHETIC benchmark city (planted-truth suite v1.0.0, regime=exposure_error)."

[thresholds]
rate_per = 1000.0
