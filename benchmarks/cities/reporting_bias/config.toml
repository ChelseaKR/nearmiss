# Generated by benchmarks/generator.py, regime "reporting_bias". Do not hand-edit;
# re-run the generator instead. Lets any tool run nearmiss itself against this
# city via:
#   nearmiss analyze --config benchmarks/cities/reporting_bias/config.toml
city = "benchmark-reporting_bias"
streets = "streets.geojson"
reports = "reports.json"
exposure = "exposure.json"
raw_dir = "/tmp/nm-bench-reporting_bias-raw"
out_dir = "/tmp/nm-bench-reporting_bias-pub"
exposure_unit = "synthetic exposure units"
dataset_note = "SYNTHETIC benchmark city (planted-truth suite v1.0.0, regime=reporting_bias)."

[thresholds]
rate_per = 1000.0
