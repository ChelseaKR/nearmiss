# Generated by benchmarks/generator.py, regime "maup_coarse". Do not hand-edit;
# re-run the generator instead. Lets any tool run nearmiss itself against this
# city via:
#   nearmiss analyze --config benchmarks/cities/maup_coarse/config.toml
city = "benchmark-maup_coarse"
streets = "streets.geojson"
reports = "reports.json"
exposure = "exposure.json"
raw_dir = "/tmp/nm-bench-maup_coarse-raw"
out_dir = "/tmp/nm-bench-maup_coarse-pub"
exposure_unit = "synthetic exposure units"
dataset_note = "SYNTHETIC benchmark city (planted-truth suite v1.0.0, regime=maup_coarse)."

[thresholds]
rate_per = 1000.0
