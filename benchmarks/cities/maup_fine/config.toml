# Generated by benchmarks/generator.py, regime "maup_fine". Do not hand-edit;
# re-run the generator instead. Lets any tool run nearmiss itself against this
# city via:
#   nearmiss analyze --config benchmarks/cities/maup_fine/config.toml
city = "benchmark-maup_fine"
streets = "streets.geojson"
reports = "reports.json"
exposure = "exposure.json"
raw_dir = "/tmp/nm-bench-maup_fine-raw"
out_dir = "/tmp/nm-bench-maup_fine-pub"
exposure_unit = "synthetic exposure units"
dataset_note = "SYNTHETIC benchmark city (planted-truth suite v1.0.0, regime=maup_fine)."

[thresholds]
rate_per = 1000.0
