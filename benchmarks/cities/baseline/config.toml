# Generated by benchmarks/generator.py, regime "baseline". Do not hand-edit;
# re-run the generator instead. Lets any tool run nearmiss itself against this
# city via:
#   nearmiss analyze --config benchmarks/cities/baseline/config.toml
city = "benchmark-baseline"
streets = "streets.geojson"
reports = "reports.json"
exposure = "exposure.json"
raw_dir = "/tmp/nm-bench-baseline-raw"
out_dir = "/tmp/nm-bench-baseline-pub"
exposure_unit = "synthetic exposure units"
dataset_note = "SYNTHETIC benchmark city (planted-truth suite v1.0.0, regime=baseline)."

[thresholds]
rate_per = 1000.0
